Basictypes should be small, atomic, hopefully standalone, types that can be reused easily
from project to project.

Basictypes should generally not actually do very much; they're mostly for 
storage.  Adding operators and so on is ok.
